# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
# brand configuration files (.env.salesplay, .env.vendrex, .env.sellmo) and personal overrides (.env)
# are NOT committed — copy .env.example to create them (see README.md)
.env
.env.*
!.env.example
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# per-tenant dev-server caches
.docusaurus-*

# generated per brand on every build/start
/static/api_spec.json
__pycache__/
