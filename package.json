{
  "name": "c-users-m4008-one-drive-nvision-attachments-desktop-my-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "cross-env DOCUSAURUS_GENERATED_FILES_DIR_NAME=.docusaurus-build docusaurus build --out-dir build/salesplay",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve --dir build/salesplay",
    "preview": "npm run build && docusaurus serve --dir build/salesplay --port 3003 --no-open",
    "preview:vendrex": "npm run build:vendrex && docusaurus serve --dir build/vendrex --port 3004 --no-open",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "build:vendrex": "cross-env TENANT=vendrex DOCUSAURUS_GENERATED_FILES_DIR_NAME=.docusaurus-build-vendrex docusaurus build --out-dir build/vendrex",
    "start:vendrex": "cross-env TENANT=vendrex DOCUSAURUS_GENERATED_FILES_DIR_NAME=.docusaurus-vendrex docusaurus start --port 3001",
    "build:sellmo": "cross-env TENANT=sellmo DOCUSAURUS_GENERATED_FILES_DIR_NAME=.docusaurus-build-sellmo docusaurus build --out-dir build/selmo",
    "start:sellmo": "cross-env TENANT=sellmo DOCUSAURUS_GENERATED_FILES_DIR_NAME=.docusaurus-sellmo docusaurus start --port 3002",
    "preview:sellmo": "npm run build:sellmo && docusaurus serve --dir build/selmo --port 3005 --no-open",
    "check": "npm run check:salesplay && npm run check:vendrex && npm run check:sellmo",
    "check:salesplay": "node tools/gio_check.mjs salesplay",
    "check:vendrex": "node tools/gio_check.mjs vendrex",
    "check:sellmo": "node tools/gio_check.mjs sellmo"
  },
  "dependencies": {
    "@docusaurus/core": "3.10.0",
    "@docusaurus/faster": "3.10.0",
    "@docusaurus/preset-classic": "3.10.0",
    "@easyops-cn/docusaurus-search-local": "^0.55.3",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "js-yaml": "^4.3.2",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.10.0",
    "@docusaurus/types": "3.10.0",
    "cross-env": "^10.1.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
